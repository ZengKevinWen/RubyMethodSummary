

1. GROUP_CONCAT函数使用
    案列： https://leetcode.cn/problems/average-time-of-process-per-machine/?envType=study-plan-v2&envId=sql-free-50



2. CASE函数实现行转换列




3. COUNT函数统计多个字段
   1. SELECT
      	id,
      	container_id,
      	container_type,
      	COUNT(DISTINCT container_id,  container_type) container_num, # 统计container_id与container_type为主键的数量(已经去除NULL)
      	COUNT(DISTINCT container_type) container_type_num,
      	COUNT(container_type) container_type_num2,
      	COUNT(DISTINCT container_id) container_num2,
      	COUNT( container_id) container_num2,
      	COUNT(*) num
      FROM
      	attachments
      WHERE
      	id BETWEEN 1
      	AND 157
   2. COUNT函数与DISTINCT函数会去重NULL的数据

4. mysql CROSS JOIN(全连接/交叉连接) CONCAT拼接多个字段关联数据 USING(attr1,attr2)  ON(多字段就用AND)
   参考文档:   1. https://leetcode.cn/problems/students-and-examinations/discussion/?envType=study-plan-v2&envId=sql-free-50    2.https://chat.openai.com/c/fbbfe2bc-4797-4223-8d8d-0efc4937a410

5. SUM与COUNT
    1. https://leetcode.cn/problems/confirmation-rate/discussion/?envType=study-plan-v2&envId=sql-free-50 2. https://chat.openai.com/c/5ea3f2d2-2251-4ddf-9f07-636a7c11f3b3