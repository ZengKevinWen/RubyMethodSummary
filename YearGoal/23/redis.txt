

8月
1. 博学谷redis基础
    第一章:redis基础

    总结:
        1. set demo  != get "demo"
        2. redis下 help的使用
        3. 具体场景使用: 1. 周期短 2. 高并发

    问题:
        1. docker创建redis容器并指定挂载在宿主机创建的redis.conf(实现redis服务课配置，取消以守护进程方式启动)
        2. docker run -v /myredis/conf:/usr/local/etc/redis --name myredis redis redis-server /usr/local/etc/redis/redis.conf 取消 --name后面的参数后应该就不会创建或者进入就启动server(带测试)

    第二章
        String类型
        1. strlen 统计string类型长度  strlen key 暂无批量操作
        2. append 追加或者创建 append key value  暂无批量操作
        3. incr key,  incrby key value,  incrbyfloat key value, decr key, decrby key value  => redis自增
        4. setex key seconds value, psetex key milliseconds value => redis控制数据时效性
        5. 场景:
            1. 实现分库分表 控制主键，防止重复
            2. 短暂存储高访问数据 => set 表名称:id:id_value:字段name value
            3. 存储时效性数据 => 当前某一商品访问热点，去重用户，根据时效性来存储热点值



9月
2. 博学谷redis进阶


3. 博学谷redis企业级实战